<% if @listing_by_movie.any? %>
$('#listings').html("<%= escape_javascript(render :partial => 'shows/listing_by_movie', :locals => {:location => @listing_by_movie})%>")
<% end %>

<% if @listing_by_theatre.any? %>
$('#listings').html("<%= escape_javascript(render :partial => 'shows/listing_by_theatre', :locals => {:location => @listing_by_theatre})%>")
<% end %>